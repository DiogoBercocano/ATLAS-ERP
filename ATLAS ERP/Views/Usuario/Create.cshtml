@model ATLAS_ERP.Models.Usuario
@{
    ViewBag.Title = "Novo Funcionário - Atlas ERP";
}

@section styles {
<style>
    .page-header { position: sticky; top: 0; z-index: 50; background: rgba(5,13,26,0.92); backdrop-filter: blur(12px); border-bottom: 1px solid var(--border); padding: 16px 36px; display: flex; align-items: center; justify-content: space-between; }
    .breadcrumb { display: flex; align-items: center; gap: 6px; font-size: 12px; color: var(--faint); }
    .breadcrumb a { color: var(--faint); text-decoration: none; transition: color 0.15s; }
    .breadcrumb a:hover { color: var(--muted); }
    .breadcrumb-sep { opacity: 0.5; }
    .breadcrumb-current { color: var(--muted); }
    .page-title-block { margin-top: 4px; }
    .btn-back { display: inline-flex; align-items: center; gap: 7px; color: var(--muted); text-decoration: none; font-size: 13px; background: var(--card); border: 1px solid var(--border); padding: 9px 16px; border-radius: 10px; transition: background 0.18s, color 0.18s; }
    .btn-back:hover { background: var(--card-hover); color: var(--text); }
    .btn-back svg { width: 14px; height: 14px; }
    .content-wrap { flex: 1; padding: 32px 36px; display: flex; gap: 28px; align-items: flex-start; }
    .form-area { flex: 1; min-width: 0; }
    .section-card { background: var(--card); border: 1px solid var(--border); border-radius: var(--radius); padding: 28px 32px; margin-bottom: 20px; animation: fadeUp 0.45s ease both; }
    .section-card:nth-child(2) { animation-delay: 0.06s; }
    .section-head { display: flex; align-items: flex-start; gap: 14px; margin-bottom: 24px; }
    .section-num { width: 28px; height: 28px; border-radius: 8px; background: var(--accent-soft); border: 1px solid rgba(59,130,246,0.25); display: flex; align-items: center; justify-content: center; font-family: 'Syne', sans-serif; font-size: 12px; font-weight: 700; color: var(--accent-light); flex-shrink: 0; margin-top: 1px; }
    .section-title { font-family: 'Syne', sans-serif; font-size: 14px; font-weight: 700; color: var(--text); margin-bottom: 2px; }
    .section-desc { font-size: 12px; color: var(--faint); }
    .form-group { margin-bottom: 20px; }
    .form-group:last-child { margin-bottom: 0; }
    .form-label { display: block; font-size: 11px; font-weight: 500; color: var(--muted); margin-bottom: 7px; text-transform: uppercase; letter-spacing: 0.06em; }
    .form-label span { color: var(--red); margin-left: 2px; }
    .input-wrapper { position: relative; }
    .input-icon { position: absolute; left: 14px; top: 50%; transform: translateY(-50%); color: var(--faint); pointer-events: none; display: flex; }
    .input-icon svg { width: 15px; height: 15px; }
    .form-control { width: 100%; height: 46px; background: var(--panel); border: 1px solid var(--border); border-radius: 10px; padding: 0 14px 0 42px; font-family: 'DM Sans', sans-serif; font-size: 14px; color: var(--text); outline: none; transition: border-color 0.2s, box-shadow 0.2s; -webkit-appearance: none; }
    .form-control::placeholder { color: var(--faint); }
    .form-control:hover { border-color: var(--border-md); }
    .form-control:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-glow); }
    select.form-control { padding-left: 42px; cursor: pointer; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    .field-error { font-size: 12px; color: var(--red); margin-top: 5px; }
    .validation-summary { background: var(--red-soft); border: 1px solid rgba(248,113,113,0.2); border-radius: 10px; padding: 12px 16px; margin-bottom: 20px; font-size: 13px; color: var(--red); }
    .password-strength-bar { height: 3px; border-radius: 2px; background: var(--border); margin-top: 8px; overflow: hidden; }
    .password-strength-fill { height: 100%; border-radius: 2px; transition: width 0.3s, background 0.3s; width: 0%; }
    .strength-hint { font-size: 11px; color: var(--faint); margin-top: 4px; }
    .preview-aside { width: 300px; flex-shrink: 0; position: sticky; top: 84px; }
    .preview-card { background: var(--card); border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; animation: fadeUp 0.5s ease 0.1s both; }
    .preview-header { background: linear-gradient(135deg, rgba(59,130,246,0.15), rgba(167,139,250,0.10)); border-bottom: 1px solid var(--border); padding: 24px; text-align: center; }
    .preview-avatar { width: 72px; height: 72px; border-radius: 50%; background: var(--accent-soft); border: 2px solid rgba(59,130,246,0.30); display: flex; align-items: center; justify-content: center; font-family: 'Syne', sans-serif; font-size: 28px; font-weight: 700; color: var(--accent-light); margin: 0 auto 14px; transition: all 0.3s; }
    .preview-name { font-family: 'Syne', sans-serif; font-size: 16px; font-weight: 700; color: var(--text); margin-bottom: 4px; min-height: 22px; }
    .preview-email { font-size: 12px; color: var(--muted); min-height: 18px; word-break: break-all; }
    .preview-body { padding: 20px 24px; }
    .preview-row { display: flex; align-items: center; gap: 10px; padding: 9px 0; border-bottom: 1px solid var(--border); font-size: 13px; }
    .preview-row:last-child { border-bottom: none; }
    .preview-row-icon { color: var(--faint); flex-shrink: 0; }
    .preview-row-icon svg { width: 14px; height: 14px; }
    .preview-row-label { font-size: 11px; color: var(--faint); min-width: 60px; }
    .preview-row-val { color: var(--text); font-weight: 500; flex: 1; }
    .preview-row-val.empty { color: var(--faint); font-style: italic; font-weight: 400; }
    .role-badge { display: inline-flex; align-items: center; padding: 3px 10px; border-radius: 20px; font-size: 11px; font-weight: 600; letter-spacing: 0.04em; text-transform: uppercase; }
    .role-badge.admin { background: var(--gold-soft); color: var(--gold); border: 1px solid rgba(201,168,76,0.2); }
    .role-badge.gerente { background: rgba(167,139,250,0.12); color: #a78bfa; border: 1px solid rgba(167,139,250,0.2); }
    .role-badge.funcionario { background: var(--accent-soft); color: var(--accent-light); border: 1px solid rgba(59,130,246,0.2); }
    .role-badge.empty-badge { background: var(--border); color: var(--faint); border: 1px solid transparent; font-style: italic; text-transform: none; }
    .preview-tip { background: rgba(59,130,246,0.06); border: 1px solid rgba(59,130,246,0.12); border-radius: 10px; padding: 14px 16px; margin-top: 14px; font-size: 12px; color: var(--faint); line-height: 1.5; }
    .preview-tip strong { color: var(--muted); }
    .action-bar { position: sticky; bottom: 0; z-index: 50; background: rgba(5,13,26,0.95); backdrop-filter: blur(12px); border-top: 1px solid var(--border); padding: 14px 36px; display: flex; align-items: center; justify-content: space-between; }
    .action-hint { font-size: 12px; color: var(--faint); }
    .action-hint span { color: var(--red); }
    .action-btns { display: flex; gap: 12px; }
    .btn-cancel { height: 44px; padding: 0 22px; background: none; border: 1px solid var(--border); border-radius: 10px; color: var(--muted); font-family: 'DM Sans', sans-serif; font-size: 14px; cursor: pointer; text-decoration: none; display: inline-flex; align-items: center; transition: background 0.18s, color 0.18s; }
    .btn-cancel:hover { background: var(--card-hover); color: var(--text); }
    .btn-submit { height: 44px; padding: 0 28px; background: var(--accent); border: none; border-radius: 10px; font-family: 'Syne', sans-serif; font-size: 14px; font-weight: 600; color: white; cursor: pointer; letter-spacing: 0.02em; transition: background 0.2s, transform 0.15s; box-shadow: 0 4px 16px rgba(59,130,246,0.25); display: inline-flex; align-items: center; gap: 8px; }
    .btn-submit:hover { background: #2563eb; transform: translateY(-1px); }
    .btn-submit:active { transform: translateY(0); }
    .btn-submit svg { width: 15px; height: 15px; }
    @@media (max-width: 1100px) { .preview-aside { display: none; } }
    @@media (max-width: 700px) { .main { margin-left: 0; } .content-wrap { padding: 20px 16px; } .page-header { padding: 14px 16px; } .action-bar { padding: 12px 16px; } .form-row { grid-template-columns: 1fr; } }
</style>
}

@section bgGlows {
<div class="bg-glow" style="width:420px;height:420px;top:-120px;right:4%;background:rgba(30,60,130,0.18);"></div>
<div class="bg-glow" style="width:280px;height:280px;bottom:10%;left:30%;background:rgba(60,30,120,0.10);"></div>
}

<main class="main">

    <div class="page-header">
        <div>
            <div class="breadcrumb">
                <a href="/Admin/Dashboard">Dashboard</a>
                <span class="breadcrumb-sep">/</span>
                <a href="/Usuario">Funcionários</a>
                <span class="breadcrumb-sep">/</span>
                <span class="breadcrumb-current">Novo</span>
            </div>
            <div class="page-title-block">
                <div class="page-title">Novo Funcionário</div>
            </div>
        </div>
        <a href="/Usuario" class="btn-back">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="15 18 9 12 15 6" />
            </svg>
            Voltar
        </a>
    </div>

    @using (Html.BeginForm("Create", "Usuario", FormMethod.Post, new { id = "formUsuario" }))
    {
        @Html.AntiForgeryToken()

        <div class="content-wrap">

            <div class="form-area">

                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="validation-summary">
                        @Html.ValidationSummary(false)
                    </div>
                }

                <div class="section-card">
                    <div class="section-head">
                        <div class="section-num">1</div>
                        <div class="section-info">
                            <div class="section-title">Identificação</div>
                            <div class="section-desc">Nome e e-mail do funcionário</div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="form-label">Nome completo <span>*</span></label>
                        <div class="input-wrapper">
                            @Html.TextBoxFor(m => m.Name, new { @class = "form-control", id = "nomeInput", placeholder = "Ex: João da Silva", autocomplete = "off" })
                            <span class="input-icon">
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                                    <circle cx="12" cy="7" r="4" />
                                </svg>
                            </span>
                        </div>
                        @Html.ValidationMessageFor(m => m.Name, "", new { @class = "field-error" })
                    </div>

                    <div class="form-group">
                        <label class="form-label">E-mail <span>*</span></label>
                        <div class="input-wrapper">
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", id = "emailInput", placeholder = "funcionario@empresa.com", type = "email" })
                            <span class="input-icon">
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <rect x="2" y="4" width="20" height="16" rx="2" />
                                    <path d="m2 7 10 7L22 7" />
                                </svg>
                            </span>
                        </div>
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "field-error" })
                    </div>
                </div>

                <div class="section-card">
                    <div class="section-head">
                        <div class="section-num">2</div>
                        <div class="section-info">
                            <div class="section-title">Acesso ao Sistema</div>
                            <div class="section-desc">Defina a senha e o nível de permissão</div>
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label">Senha <span>*</span></label>
                            <div class="input-wrapper">
                                @Html.PasswordFor(m => m.SenhaHash, new { @class = "form-control", id = "senhaInput", placeholder = "••••••••" })
                                <span class="input-icon">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                        <rect x="3" y="11" width="18" height="11" rx="2" />
                                        <path d="M7 11V7a5 5 0 0 1 10 0v4" />
                                    </svg>
                                </span>
                            </div>
                            <div class="password-strength-bar">
                                <div class="password-strength-fill" id="strengthFill"></div>
                            </div>
                            <div class="strength-hint" id="strengthHint"></div>
                            @Html.ValidationMessageFor(m => m.SenhaHash, "", new { @class = "field-error" })
                        </div>

                        <div class="form-group">
                            <label class="form-label">Cargo <span>*</span></label>
                            <div class="input-wrapper">
                                @{
                                    var cargos = ViewBag.Cargos as List<ATLAS_ERP.Models.Cargo> ?? new List<ATLAS_ERP.Models.Cargo>();
                                }
                                @Html.DropDownListFor(m => m.CargoId,
                                    new SelectList(cargos, "CargoId", "Nome"),
                                    "Selecione um cargo...",
                                    new { @class = "form-control", id = "cargoSelect" }
                                )
                                <span class="input-icon">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                        <path d="M12 2a10 10 0 1 0 0 20A10 10 0 0 0 12 2z" />
                                        <path d="M12 8v4l3 3" />
                                    </svg>
                                </span>
                            </div>
                            @Html.ValidationMessageFor(m => m.CargoId, "", new { @class = "field-error" })
                        </div>
                    </div>
                </div>

            </div>

            <aside class="preview-aside">
                <div class="preview-card">
                    <div class="preview-header">
                        <div class="preview-avatar" id="prevAvatar">?</div>
                        <div class="preview-name" id="prevNome">Nome do funcionário</div>
                        <div class="preview-email" id="prevEmail">email@empresa.com</div>
                    </div>
                    <div class="preview-body">
                        <div class="preview-row">
                            <div class="preview-row-icon">
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <path d="M12 2a10 10 0 1 0 0 20A10 10 0 0 0 12 2z" />
                                    <path d="M12 8v4l3 3" />
                                </svg>
                            </div>
                            <div class="preview-row-label">Cargo</div>
                            <div class="preview-row-val" id="prevRole">
                                <span class="role-badge empty-badge">não definido</span>
                            </div>
                        </div>
                        <div class="preview-row">
                            <div class="preview-row-icon">
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <rect x="3" y="11" width="18" height="11" rx="2" />
                                    <path d="M7 11V7a5 5 0 0 1 10 0v4" />
                                </svg>
                            </div>
                            <div class="preview-row-label">Senha</div>
                            <div class="preview-row-val" id="prevSenha">
                                <span class="empty">não definida</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="preview-tip">
                    <strong>Dica:</strong> O funcionário poderá alterar sua senha após o primeiro acesso. Escolha um cargo adequado às responsabilidades.
                </div>
            </aside>

        </div>

        <div class="action-bar">
            <div class="action-hint"><span>*</span> Campos obrigatórios</div>
            <div class="action-btns">
                <a href="/Usuario" class="btn-cancel">Cancelar</a>
                <button type="submit" class="btn-submit">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" />
                        <circle cx="9" cy="7" r="4" />
                        <line x1="19" y1="8" x2="19" y2="14" />
                        <line x1="22" y1="11" x2="16" y2="11" />
                    </svg>
                    Criar Funcionário
                </button>
            </div>
        </div>
    }

</main>

@section scripts {
<script nonce="@Html.CspNonce()">
    var nomeInput  = document.getElementById('nomeInput');
    var emailInput = document.getElementById('emailInput');
    var senhaInput = document.getElementById('senhaInput');
    var cargoSelect = document.getElementById('cargoSelect');

    var prevAvatar = document.getElementById('prevAvatar');
    var prevNome   = document.getElementById('prevNome');
    var prevEmail  = document.getElementById('prevEmail');
    var prevRole   = document.getElementById('prevRole');
    var prevSenha  = document.getElementById('prevSenha');
    var strengthFill = document.getElementById('strengthFill');
    var strengthHint = document.getElementById('strengthHint');

    function updatePreview() {
        var nome = nomeInput.value.trim();
        if (nome) {
            prevAvatar.textContent = nome.charAt(0).toUpperCase();
            prevNome.textContent = nome;
            prevNome.classList.remove('empty');
        } else {
            prevAvatar.textContent = '?';
            prevNome.innerHTML = 'Nome do funcionário';
            prevNome.classList.add('empty');
        }

        var email = emailInput.value.trim();
        if (email) {
            prevEmail.textContent = email;
            prevEmail.classList.remove('empty');
        } else {
            prevEmail.textContent = 'email@empresa.com';
            prevEmail.classList.add('empty');
        }

        var cargoSelected = cargoSelect.options[cargoSelect.selectedIndex];
        prevRole.textContent = '';
        var roleBadge = document.createElement('span');
        if (cargoSelected && cargoSelected.value) {
            roleBadge.className = 'role-badge';
            roleBadge.textContent = cargoSelected.text;
        } else {
            roleBadge.className = 'role-badge empty-badge';
            roleBadge.textContent = 'não definido';
        }
        prevRole.appendChild(roleBadge);

        var pwd = senhaInput.value;
        if (!pwd) {
            prevSenha.innerHTML = '<span class="empty">não definida</span>';
            strengthFill.style.width = '0%';
            strengthFill.style.background = '';
            strengthHint.textContent = '';
        } else {
            var score = 0;
            if (pwd.length >= 8)  score++;
            if (/[A-Z]/.test(pwd)) score++;
            if (/[0-9]/.test(pwd)) score++;
            if (/[^A-Za-z0-9]/.test(pwd)) score++;

            var levels = [
                { pct: '25%',  color: '#f87171', label: 'Fraca' },
                { pct: '50%',  color: '#fb923c', label: 'Razoável' },
                { pct: '75%',  color: '#facc15', label: 'Boa' },
                { pct: '100%', color: '#34d399', label: 'Forte' }
            ];
            var lvl = levels[Math.max(0, score - 1)];
            strengthFill.style.width  = lvl.pct;
            strengthFill.style.background = lvl.color;
            strengthHint.textContent  = 'Senha ' + lvl.label;
            strengthHint.style.color  = lvl.color;

            var dots = '';
            for (var i = 0; i < pwd.length && i < 12; i++) dots += '•';
            if (pwd.length > 12) dots += '…';
            prevSenha.innerHTML = '<span style="letter-spacing:2px;">' + dots + '</span> <span style="font-size:11px;color:var(--faint);">(' + lvl.label + ')</span>';
        }
    }

    nomeInput.addEventListener('input',  updatePreview);
    emailInput.addEventListener('input', updatePreview);
    senhaInput.addEventListener('input', updatePreview);
    cargoSelect.addEventListener('change', updatePreview);

    updatePreview();
</script>
}
